import type { WeaponDefinition } from './CombatTypes';

export const trainingRifle: WeaponDefinition = {
  id: 'training-rifle',
  name: 'Training Rifle',
  fireIntervalMs: 150,
  damage: 100,
  range: 80,
  automatic: false,
};
